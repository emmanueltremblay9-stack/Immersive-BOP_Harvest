# Canonical source bytes keep the manifest ledger stable on Windows and Linux.
* text=auto eol=lf
*.jar -text
*.png -text
*.nbt -text
# These generated resources are already canonical LF.
src/generated/**/.cache/* text eol=lf
src/generated/**/*.json text eol=lf
